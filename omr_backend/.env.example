# OMR backend ortam değişkenleri (örnek). Gerçek .env repoya KONMAZ.

# Cache klasörü (dosya bazlı OMR sonucu önbelleği)
OMR_CACHE_DIR=/tmp/omr_cache

# Render/PaaS port'u sağlar; yerelde uvicorn --port ile verilir.
# PORT=8000
